{
  "name": "opus-garden",
  "type": "module",
  "version": "0.0.1",
  "engines": {
    "node": ">=22.12.0"
  },
  "scripts": {
    "dev": "astro dev",
    "build": "astro build",
    "preview": "bun run build && wrangler dev",
    "deploy": "bun run build && wrangler deploy"
  },
  "dependencies": {
    "@tailwindcss/vite": "^4.2.3",
    "astro": "^7.2.0",
    "tailwindcss": "^4.2.3"
  },
  "devDependencies": {
    "wrangler": "^4.84.0"
  }
}